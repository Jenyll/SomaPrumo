export type OrganizationId = string

export interface Organization {
                    id: OrganizationId
                    name: string
                    slug: string
                    active: boolean
}
